services:
  db:
    ports:
      - "${DB_PORT:-5432}:5432"

  app:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - DOTNET_WATCH_RESTART_ON_RUDE_EDIT=true
    ports:
      - "127.0.0.1:8080:8080"
      - "127.0.0.1:5000:5000"
    volumes:
      - ./src/Merconiq.Web:/src/src/Merconiq.Web:ro
      - ./src/Merconiq.Core:/src/src/Merconiq.Core:ro
      - ./src/Merconiq.Infrastructure:/src/src/Merconiq.Infrastructure:ro
      - app-logs:/app/logs
    develop:
      watch:
        - action: rebuild
          path: ./src/Merconiq.Web
        - action: rebuild
          path: ./src/Merconiq.Core
        - action: rebuild
          path: ./src/Merconiq.Infrastructure

volumes:
  app-logs:
    driver: local
